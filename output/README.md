# Output Directory

This directory stores files created by the analysis scripts.

- `results/`: `.RData` files written by the experiment scripts.
- `figures/`: figures created by `code/plot.R`.

These subdirectories are included so the repository can be uploaded directly and populated as analyses are run.
