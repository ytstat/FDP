# Data Directory

This directory stores the data files used by the empirical analyses in the paper.

- `Exam_Score_Prediction.csv`: input data for `code/experiments/exam.R` and `code/experiments/exam-outlier.R`.

The remaining experiment scripts in this repository generate synthetic data internally and therefore do not require additional files in this directory.
